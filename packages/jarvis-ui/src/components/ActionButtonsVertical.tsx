import React from 'react'

interface Props { className?: string }

export const ActionButtonsVertical: React.FC<Props> = ({ className }) => (
  <div className={className}>ActionButtonsVertical</div>
)
