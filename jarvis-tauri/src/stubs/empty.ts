// Empty stub — used by Vite alias to replace node-only modules
// (better-sqlite3, @mapbox/node-pre-gyp) that leak through the
// jarvis-core import graph but are never called in the browser.
export default {};
export {};
